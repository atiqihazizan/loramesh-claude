# node -e "console.log(require('crypto').randomBytes(48).toString('base64'))"
# ====================================
# backend_claude — environment template
# Copy to .env and fill in. Never commit .env
# ====================================

# --- Server ---
NODE_ENV=production
PORT=5002
TIMEZONE=Asia/Kuala_Lumpur

# --- Database (single unified DB) ---
# DATABASE_URL=mysql://root:m.atiqi1985@127.0.0.1:3306/lora_mesh_pro
DATABASE_URL=mysql://root:root@localhost:3306/lora_mesh_pro

# --- Legacy DB (untuk migration sahaja) ---
LEGACY_DATABASE_URL=mysql://root:m.atiqi1985@178.128.48.114:3306/loraDB

# --- Auth ---
# Frontend JWT
JWT_SECRET=ffBVGIVSDKWJYl7/JrTs3zgu936HPxNzo0oqNQju9yyKyuyl67f5rAXNHI4+pisR
JWT_EXPIRES_IN=7d
BCRYPT_ROUNDS=10

# --- CORS ---
# Comma-separated list of allowed origins
CORS_ORIGIN=https://lora2u.com,https://www.lora2u.com,http://localhost:5173

# --- Socket.IO ---
# false = device-tracking with filter/threshold (production)
# true  = device-tracking-raw without filter (debug only)
USE_RAW_TRACKING=false
TRACKING_EMIT_VERBOSE=false

# --- MQTT (Flutter modbus_go + LoRa Gateway) ---
MQTT_ENABLED=true
MQTT_HOST=mahsites.net
MQTT_PORT=8887
MQTT_PROTOCOL=wss
MQTT_BASEPATH=ws
MQTT_USERNAME=wsmqtt
MQTT_PASSWORD=w5mqtt

# MQTT tuning
MQTT_PROCESS_CONCURRENCY=8
MQTT_SUBSCRIBE_QOS=2
MQTT_MOBILE_SUBSCRIBE_QOS=1
MQTT_LOG_INCOMING_DB=true
MQTT_VERBOSE_LOG=false

# --- Tracking throttle ---
# DB write: live_tracking upsert throttled per device (playback always written)
TRACKING_DB_THROTTLE_MS=3000
# Socket.IO broadcast throttled per device
TRACKING_BROADCAST_THROTTLE_MS=2000

# --- Heartbeat (device offline detection) ---
MODBUSGO_HEARTBEAT_TIMEOUT=60000
LORA_HEARTBEAT_TIMEOUT=120000

# --- Internal API base URL (MQTT handler → REST self-call) ---
API_BASE_URL=https://lora2u.com

# --- Provisioning ---
# Nonce time-to-live in minutes (default: 1 day)
PROVISIONING_NONCE_TTL_MIN=1440
PROVISION_ENCRYPTION_KEY=da9dece0aeefdc38f021d7cee0356e9f8

# --- Rate limit ---
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX_REQUESTS=1000
MQTT_WSS_HOST=mahsites.net
MQTT_WSS_PORT=8887
